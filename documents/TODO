Todo
====

A-Z variables. 
Add standard Unary functions handler include ( ! ? & @ and len()
Complete unary functions (integer only)
Write basic runner, and start 
Assert and testing framework.
String unary and support (concat, compare etc.) when testing frameowork up

Incomplete but hooked in for write and test:	
	Variable Access
	String Concatenation, Compare
	FP Import from token, int/float conversions, add/sub/mul/div/compare

Notes:
	Stack position fudged for readability.